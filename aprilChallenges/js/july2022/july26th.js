const today = new Date
console.log(today)


// function squareDigits(num) {
//     const numArr = num.toString().split('')
//     const returnArr = []
//     numArr.forEach(num => {
//         console.log(num)
//         returnArr.push(+num * +num)
//     })
//     return returnArr.join('')
// }

// console.log(squareDigits(3212))

// --------------------------------------------//

//FizzBuzz -

// return an array, from 1 -> n (param)
    //replace certain values. 
    //

    // If the value is a multiple of 3: use the value "Fizz" instead
    // If the value is a multiple of 5: use the value "Buzz" instead
    // If the value is a multiple of 3 & 5: use the value "FizzBuzz" instead
    // N will never be less than 1.

// 

// take a number and pass that number into a loop. loop starts at 0 will run to the number specified in our paramater. 
    // take the number and check if its multiple 5 and 3! and then if its a multiple of 3, then 5. 
    //push to an empty array. 

function fizzbuzz(n) {
    

}