const today = new Date
console.log(today)

// 8kyu: returning strings: 

// create a function that returns a greeting that uses an input. 
// I going to create a function that accepts a string input as its paramater. PAssing that input into a string literal that outputs the same string but changes the name based on the input passed in. 

const greet = n => `Hello, ${n} how are you doing today?`